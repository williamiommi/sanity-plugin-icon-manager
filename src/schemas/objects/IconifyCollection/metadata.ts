interface IconifyCollectionMetadataProps {
  type: 'object'
  name: string
  title: string
}

const IconifyCollectionMetadata: IconifyCollectionMetadataProps = {
  type: 'object',
  name: 'iconify.collection',
  title: 'Iconify Collection',
}

export default IconifyCollectionMetadata
